# 📈 Stock Forecast Telegram Bot

Telegram-бот для анализа и прогнозирования цен акций на основе временных рядов.

## 🚀 Возможности

- 📊 Загрузка исторических данных с Yahoo Finance
- 🤖 Обучение трех типов моделей:
  - Random Forest (ML)
  - ARIMA (статистическая)
  - LSTM (нейросетевая)
- 📈 Автоматический выбор лучшей модели
- 🔮 Прогноз на 30 дней вперед
- 💰 Рекомендации по покупке/продаже
- 📉 Расчет потенциальной прибыли
- 📝 Детальное логирование всех запросов

## ⚙️ Установка

1. Клонируйте репозиторий:
```bash
git clone <your-repo>
cd stock_forecast_bot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл, добавив ваш токен бота
```

4. Получите токен бота у [@BotFather](https://t.me/BotFather)

5. Запустите бота:
```bash
python main.py
```

## 📋 Использование

1. Запустите бота в Telegram
2. Нажмите "Получить прогноз акций"
3. Введите тикер компании (например: `AAPL`)
4. Введите сумму для условной инвестиции
5. Дождитесь результатов анализа

## 🏗️ Архитектура

Проект построен по модульной архитектуре:

```
controllers/          # Контроллеры бота
models/              # ML-модели
services/            # Бизнес-логика
  ├── data_service.py     # Работа с данными
  ├── model_selector.py   # Выбор модели
  ├── analytics_service.py # Аналитика
  ├── plot_service.py     # Визуализация
  └── log_service.py      # Логирование
utils/               # Вспомогательные функции
config.py           # Конфигурация
main.py             # Точка входа
```

## 📊 Примеры тикеров

- `AAPL` - Apple Inc.
- `MSFT` - Microsoft
- `GOOGL` - Alphabet (Google)
- `TSLA` - Tesla
- `AMZN` - Amazon
- `META` - Meta Platforms
- `NVDA` - NVIDIA

## ⚠️ Важное предупреждение

**Это учебный проект!** Все прогнозы и рекомендации носят исключительно демонстрационный характер и не должны использоваться для реальных инвестиционных решений.

## 🧪 Тестирование

Для запуска тестов:
```bash
pytest tests/
```

## 📝 Логирование

Все запросы пользователей логируются в `logs.csv` с детальной информацией:
- Временная метка
- ID пользователя
- Тикер
- Сумма инвестиции
- Выбранная модель
- Метрики качества
- Рассчитанная прибыль

## 🔧 Настройка

Основные параметры можно настроить в `.env` файле:

```env
BOT_TOKEN=ваш_токен
HISTORICAL_YEARS=2      # Годы исторических данных
FORECAST_DAYS=30        # Дней прогноза
YAHOO_CACHE_DIR=./cache # Директория кэша
LOG_FILE=./logs.csv     # Файл логов
```

## 📈 Метрики качества

Модели сравниваются по:
- **RMSE** (Root Mean Square Error)
- **MAPE** (Mean Absolute Percentage Error)
- **MAE** (Mean Absolute Error)